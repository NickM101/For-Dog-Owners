export const convertNaming = text => {
  const nameArr = text.split(' ');
  for (let letters = 0; letters < array.length; letters++) {
    const element = array[letters];
  }
  console.log('text', nameArr);
  const newName = nameArr.join(' ');
  console.log('newName:', newName);

  return newName;
};
